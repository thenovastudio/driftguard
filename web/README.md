# DriftGuard — Web Dashboard

Next.js frontend for DriftGuard.

## Development

```bash
npm install
npm run dev
```

Open http://localhost:3001

The dev server proxies API requests to `localhost:3000` (the backend).
